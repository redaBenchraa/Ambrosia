spring:
  liquibase:
    enabled: false
  datasource:
    initialization-mode: embedded
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
